// ── Layout defaults ───────────────────────────────────────────────────────────
export const DEFAULT_CARD_W = 220;
export const DEFAULT_CARD_H = 180;
export const LAYOUT_GAP = 8;
export const DEFAULT_ROW_H = 200;
export const DEFAULT_COL_W = 200;

// ── Panel constraints ─────────────────────────────────────────────────────────
export const LEFT_PANEL_MIN_W = 180;
export const LEFT_PANEL_MAX_W = 480;

// ── Canvas fallback ───────────────────────────────────────────────────────────
export const CANVAS_FALLBACK_W = 900;
export const CANVAS_FALLBACK_H = 600;

// ── Undo history ──────────────────────────────────────────────────────────────
export const MAX_UNDO_STEPS = 50;

// ── Z-index layers ────────────────────────────────────────────────────────────
export const Z_FULLSCREEN = 1000;
export const Z_RESIZE_HANDLE = 10;

// ── Brand ─────────────────────────────────────────────────────────────────────
export const BRAND_NAME = 'SUPERSET';
export const DEFAULT_TEMPLATE_NAME = 'My Dashboard';
